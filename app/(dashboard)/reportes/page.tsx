import { createClient } from "@/lib/supabase/server";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { KpiCard } from "@/components/dashboard/kpi-card";
import { formatCurrency } from "@/lib/utils";
export default async function ReportesPage() { const supabase = createClient(); const [{ count: ventas }, { count: perdidas }, { data: vendidas }] = await Promise.all([supabase.from("movimientos").select("id", { count: "exact", head: true }).eq("tipo", "venta"), supabase.from("movimientos").select("id", { count: "exact", head: true }).in("tipo", ["vencimiento", "perdida"]), supabase.from("unidades_inventario").select("producto:productos(precio)").eq("estado", "vendida")]); const ingresos = (vendidas ?? []).reduce((acc:any, u:any) => acc + Number(u.producto?.precio ?? 0), 0); return <div className="space-y-6"><div><h1 className="text-3xl font-semibold tracking-tight">Reportes</h1><p className="text-muted-foreground">Indicadores comerciales y pérdidas.</p></div><div className="grid gap-4 sm:grid-cols-3"><KpiCard title="Ventas registradas" value={ventas ?? 0} /><KpiCard title="Pérdidas/vencimientos" value={perdidas ?? 0} /><KpiCard title="Ingresos estimados" value={formatCurrency(ingresos)} /></div><Card><CardHeader><CardTitle>Próxima mejora</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">Agregar filtros por fechas, punto y producto; gráficas por producto más vendido y puntos con más pérdidas.</CardContent></Card></div>; }
